import { useParams } from 'react-router-dom';
import { Wrapper_detailTeam } from './teamDetailStyle';
import { teamMember } from '../../mock/teamDate';

const TeamDetail_component = () => {
    // const { id } = useParams();
    // const parseInt = id ? parseInt(id) : 0;
    // const data = teamMember.detail;
    // console.log(data);
    // console.log(id);

    // const filterData = data.filter((value) => value.id === parseInt);
 
    // console.log(filterData);
    
    
  return (
    <Wrapper_detailTeam>
        <p>sadasdasdasdasd</p>
    </Wrapper_detailTeam>
  )
}

export default TeamDetail_component;